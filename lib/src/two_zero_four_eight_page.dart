part of '../two_zero_four_eight.dart';

class TwoZeroFourEightPage extends StatelessWidget {
  const TwoZeroFourEightPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('2048')),
      body: const Center(child: Text('Coming soon — see PLAN.md')),
    );
  }
}
